---
apiVersion: featuremanagement.cloudbees.io/v1alpha1
kind: flag-configuration

flag: default.LivestreamFeatureDirect

enabled: true

stickinessProperty: rox.distinct_id

defaultValue: false

conditions:
  - allOf:
      - property:
          name: contentPartner
          operator: eq
          operands:
            - 216
    flagValue: true
